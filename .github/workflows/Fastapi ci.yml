name: FastAPI CI Step 4 (Integration Test)

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

jobs:
  ci-check:
    name: Lint + Format + Tests + Integration
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout Repo
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3. Install Dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip

          # Project dependencies
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

          # CI tools
          pip install flake8 black pytest pytest-cov httpx

      # 4. Lint Check (soft)
      - name: Run flake8
        run: |
          flake8 . --max-line-length=120 --ignore=W291,W293,W292 || true

      # 5. Format Check
      - name: Run Black Check
        run: |
          black . --check || true

      # 6. Run Unit Tests + Coverage
      - name: Run Tests with Coverage
        run: |
          pytest --cov=. --cov-report=term-missing -v
                
      - name: Start FastAPI server
        run: |
          uvicorn main:app --host 0.0.0.0 --port 8000 > server.log 2>&1 &
          sleep 3
          cat server.log

      - name: Test /health endpoint
        run: |
          for i in {1..10}; do
            curl -f http://127.0.0.1:8000/health && break
            echo "Waiting..."
            sleep 2
          done
          echo "✅ Integration Test Passed!"