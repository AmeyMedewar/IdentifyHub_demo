name: Admin Frontend CI

on:
  push:
    branches: [ dev ]
    paths:
      - "Admin_Frontend/**"
      - ".github/workflows/admin-frontend-ci.yml"

  pull_request:
    branches: [ dev, main ]
    paths:
      - "Admin_Frontend/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"
          cache-dependency-path: "Admin_Frontend/Admin Dashboard/project/package-lock.json"

      # 3. Install dependencies
      - name: Install dependencies
        working-directory: "Admin_Frontend/Admin Dashboard/project"
        run: npm ci

      # 4. Build project
      - name: Build project
        working-directory: "Admin_Frontend/Admin Dashboard/project"
        run: npm run build

      # 5. Verify build output
      - name: Verify build output
        run: |
          if [ -d "Admin_Frontend/Admin Dashboard/project/dist" ]; then
            echo "✅ Build successful - dist folder created"
            ls -la "Admin_Frontend/Admin Dashboard/project/dist/"
          else
            echo "❌ Build failed - no dist folder"
            exit 1
          fi

      # 6. Run tests (if available)
      - name: Run tests (if available)
        working-directory: "Admin_Frontend/Admin Dashboard/project"
        run: |
          if grep -q '"test"' package.json; then
            echo "Running tests..."
            npm test -- --passWithNoTests
          else
            echo "⚠️ No test script found in package.json"
          fi
        continue-on-error: true
