name: Admin Backend CI

on:
  push:
    branches: [dev]
    paths:
      - "Admin_Backend/**"
      - ".github/workflows/admin-backend-ci.yml"

  pull_request:
    branches: [dev, main]
    paths:
      - "Admin_Backend/**"

jobs:
  build-admin-backend:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Java
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin
          cache: maven

      # 3. Build + Test
      - name: Build & Run Tests (Admin Backend)
        working-directory: Admin-Backend
        run: |
          echo "🚀 Building Admin Backend..."
          mvn clean verify

      # 4. Generate Jacoco Coverage Report
      - name: Generate Coverage Report
        working-directory: Admin-Backend
        run: mvn jacoco:report
        continue-on-error: true

      # 5. Upload Test Reports
      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: admin-backend-test-results
          path: Admin-Backend/target/surefire-reports/

      # 6. Upload Coverage Report
      - name: Upload Coverage Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: admin-backend-jacoco-report
          path: Admin-Backend/target/site/jacoco/
